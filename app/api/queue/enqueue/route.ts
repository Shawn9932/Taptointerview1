import { NextResponse } from 'next/server'; import { Store } from '../../../../shared/store'; export async function POST(req:Request){ const {jobId,email}=await req.json(); if(!jobId) return NextResponse.json({error:'jobId required'},{status:400}); const queueId=Store.uuid(); Store.queue.push({queueId,jobId,createdAt:Date.now(),status:'waiting',sessionId:null,email}); return NextResponse.json({queueId}); }
