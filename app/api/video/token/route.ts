import { NextResponse } from 'next/server'; export async function POST(){ const {TWILIO_ACCOUNT_SID,TWILIO_API_KEY_SID,TWILIO_API_KEY_SECRET}=process.env as Record<string,string|undefined>; const roomName='open-interview-beta'; if(!TWILIO_ACCOUNT_SID||!TWILIO_API_KEY_SID||!TWILIO_API_KEY_SECRET){ return NextResponse.json({ token:'mock-dev-token', roomName, note:'Provide Twilio env vars for real token' }); } return NextResponse.json({ token:'TODO_REAL_TWILIO_TOKEN', roomName }); }
