import { NextResponse } from 'next/server'; import { Store } from '../../../shared/store'; export async function POST(req:Request){ const {sessionId,outcome,notes}=await req.json(); const s=Store.sessions.find(s=>s.sessionId===sessionId); if(!s) return NextResponse.json({error:'not found'},{status:404}); s.outcome=outcome; s.notes=notes; s.endedAt=Date.now(); return NextResponse.json({ok:true}); }
